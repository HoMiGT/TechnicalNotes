# 一、自定义损失函数
PyTorch在torch.nn模块中提供了许多常用的损失函数    
自定义损失函数
## 1. 以函数方式定义
损失函数仅仅是一个函数而已， 因此可以通过直接以函数定义的方式定义一个自己的函数
```
def my_loss(output, target):
    loss = (output - target).abs().mean()
    return loss
```
## 2. 以类方式定义
以函数的定义更简单，但以类的定义更加常用，以类定义损失函数时，我们看到每一个损失函数的继承关系
```
class DiceLoss(nn.Module):
    def __init__(self, weight=None, size_average=True):
        super(DiceLoss, self).__init__()
    def forward(self, inputs, targets, smooth=1):
        inputs = F.sigmoid(inputs)
        inputs = inputs.view(-1)
        targets = targets.view(-1)
        intersection = (inputs * targets).sum()
        dice = (2. * intersection + smooth) / (inputs.sum() + targets.sum() + smooth)
        return 1 - dice
criterion = DiceLoss()
loss = criterion(inputs, targets)
```
除此之外， 常见的损失函数还有BCE-DiceLoss、IoU Loss、Focal Loss等， 这些损失函数都是以类的方式定义的。
```
class DiceBCELoss(nn.Module):
    def __init__(self, weight=None, size_average=True):
        super(DiceBCELoss, self).__init__()
        
    def forward(self, inputs, targets, smooth=1):
        inputs = F.sigmoid(inputs)
        inputs = inputs.view(-1)
        targets = targets.view(-1)
        intersection = (inputs * targets).sum()
        dice_loss = 1 - (2. * intersection + smooth) / (inputs.sum() + targets.sum() + smooth)
        BCE = F.binary_cross_entropy(inputs, targets, reduction='mean')
        return BCE + dice_loss
```
```
class IoULoss(nn.Module):
    def __init__(self, weight=None, size_average=True):
        super(IoULoss, self).__init__()
        
    def forward(self, inputs, targets, smooth=1):
        inputs = F.sigmoid(inputs)
        inputs = inputs.view(-1)
        targets = targets.view(-1)
        intersection = (inputs * targets).sum()
        total = (inputs + targets).sum()
        union = total - intersection
        IoU = (intersection + smooth) / (union + smooth)
        return 1 - IoU
```
```
ALPHA = 0.8
GAMMA = 2 
class FocalLoss(nn.Module):
    def __init__(self, weight=None, size_average=True):
        super(FocalLoss, self).__init__()

    def forward(self, inputs, targets, alpha=ALPHA, gamma=GAMMA):
        inputs = F.sigmoid(inputs)
        inputs = inputs.view(-1)
        targets = targets.view(-1)
        BCE = F.binary_cross_entropy(inputs, targets, reduction='mean')
        BCE_EXP = torch.exp(-BCE)
        focal_loss = alpha * (1 - BCE_EXP) ** gamma * BCE
        return focal_loss
```
在定义的损失函数时，涉及到数学运算时，最好全程使用PyTorch提供的张量计算接口，
这样就不需要我们实现自动求导功能并且我们可以直接调用cuda，使用numpy或scipy的数学运算时，
操作会有些麻烦。

# 二、动态调整学习率
学习率的选择是深度学习中一个困扰人们许久的问题，学习速率设置过小，会极大降低收敛速度，增加训练时间；
学习率太大，可能会导致参数在最优解两侧来回震荡。
## 1. 使用官方Scheduler 
PyTorch提供了torch.optim.lr_scheduler模块，提供了多种学习率调整策略，常用的有：
- lr_scheduler.LambdaLR
- lr_scheduler.MultiplicativeLR
- lr_scheduler.StepLR
- lr_scheduler.MultiStepLR
- lr_scheduler.ExponentialLR
- lr_scheduler.CosineAnnealingLR
- lr_scheduler.ReduceLROnPlateau
- lr_scheduler.CyclicLR
- lr_scheduler.OneCycleLR
- lr_scheduler.CosineAnnealingWarmRestarts
- lr_scheduler.ConstantLR
- lr_scheduler.LinearLR
- lr_scheduler.PolynomialLR
- lr_scheduler.ChainedScheduler
- lr_scheduler.SequentialLR
## 2. 自定义scheduler
```
def adjust_learning_rate(optimizer, epoch):
    lr = args.lr * (0.1 ** (epoch // 30))
    for param_group in optimizer.param_groups:
        param_group['lr'] = lr
optimizer = torch.optim.SGD(model.parameters(), lr=args.lr, momentum=0.9)
for epoch in range(100):
    train(...)
    validate(...)
    adjust_learning_rate(optimizer, epoch)
```

# 三、模型微调
## 1. torchvision
收集和标注数据会花费大量的时间和资金。     
迁移学习，将从源数据学习到的知识迁移到目标数据集上。     
迁移学习的一大应用场景是模型微调。     
在PyTorch中提供了许多预训练好的网络模型(VGG、ResNet、DenseNet、Inception等)，我们可以直接使用这些预训练好的模型进行微调。
### 1. 模型微调的流程
1. 在源数据集上预训练一个神经网络模型，即源模型
2. 创建一个新的神经网络模型，即目标模型。 它复制了源模型上除了输出层外的所有模型设计及其参数。
3. 为目标模型添加一个输出大小为目标数据集类别个数的输出层，并随机初始化该层的模型参数。
4. 在目标数据集上训练目标模型。
### 2. 使用已有模型结构
实例化网络
```
import torchvision.models as models
resnet18 = models.resnet18()
# resnet18 = models.resnet18(pretrained=False)  等价于与上面的表达式
alexnet = models.alexnet()
vgg16 = models.vgg16()
squeezenet = models.squeezenet1_0()
densenet = models.densenet161()
inception = models.inception_v3()
googlenet = models.googlenet()
shufflenet = models.shufflenet_v2_x1_0()
mobilenet_v2 = models.mobilenet_v2()
mobilenet_v3_large = models.mobilenet_v3_large()
mobilenet_v3_small = models.mobilenet_v3_small()
resnext50_32x4d = models.resnext50_32x4d()
wide_resnet50_2 = models.wide_resnet50_2()
mnasnet = models.mnasnet1_0()
```
传递pretrained参数, linux和mac在.cache下，windows在C:\Users\用户名\.cache\torch\hub\checkpoints
```
import torchvision.models as models
resnet18 = models.resnet18(pretrained=True)
alexnet = models.alexnet(pretrained=True)
squeezenet = models.squeezenet1_0(pretrained=True)
vgg16 = models.vgg16(pretrained=True)
densenet = models.densenet161(pretrained=True)
inception = models.inception_v3(pretrained=True)
googlenet = models.googlenet(pretrained=True)
shufflenet = models.shufflenet_v2_x1_0(pretrained=True)
mobilenet_v2 = models.mobilenet_v2(pretrained=True)
mobilenet_v3_large = models.mobilenet_v3_large(pretrained=True)
mobilenet_v3_small = models.mobilenet_v3_small(pretrained=True)
resnext50_32x4d = models.resnext50_32x4d(pretrained=True)
wide_resnet50_2 = models.wide_resnet50_2(pretrained=True)
mnasnet = models.mnasnet1_0(pretrained=True)
```
### 3. 训练特定层
```
def set_parameter_requires_grad(model, feature_extracting):
    if feature_extracting:
        for param in model.parameters():
            param.requires_grad = False
import torchvision.models as models 
feature_extract = True 
model = models.resnet18(pretrained=True)
set_parameter_requires_grad(model, feature_extract)
# 修改模型
num_ftrs = model.fc.in_features
model.fc = nn.Linear(in_features=num_ftrs, out_features=4, bias=True)

```
## 2. timm
[timm github](https://github.com/huggingface/pytorch-image-models)

### 1. timm的安装
```
pip install timm
```
### 2. 查看预训练模型种类
```
import timm 
avail_pretrained_models = timm.list_models(pretrained=True)
len(avail_pretrained_models)
```

# 四、半精度训练
GPU的性能主要分为两部分：算力和显存。    
算力决定了显卡计算的速度。    
显存决定了同时放入多少数据用于计算。    

PyTorch默认存储方式用的是torch.float32, 也就是单精度浮点数。但大多数场景其实并不需要这么精确，只保留一半的信息也不会影响结果
即使用torch.float16, 也就是半精度浮点数。

## 1. 半精度训练的设置
在PyTorch中，使用autocast配置半精度训练，同时需要在下面三处加以设置:
* import autocast
```
from torch.cuda.amp import autocast
```
* 模型设置
```
@autocast()
def forward(self, x):
    ...
    return x 
```
* 训练过程
```
for x in train_loader:
    x = x.cuda()
    with autocast():
        output = model(x)
```
注意：
    半精度训练主要适用于数据本身的size比较大。当数据本身的size并不大时，使用半精度训练则可能不会带来显著的提升。

# 五、数据增强-Albumentations
[Albumentations](https://albumentations.ai/docs/#getting-started)
```
pip install albumentationsx
```
## 1. 核心概念
### 1.1 变换
关键概念:
* 单次操作：每次变化执行一次特定的增强操作
* 概率控制：p参数控制应用可用性(0.0-1.0)
* 参数抽样：每次从指定范围内选择随机值
* 变化类型：像素变换和空间变换
* 目标兼容性：某些变换会影响多种数据类型(图像、掩码、边界框、关键点)

首先要进行的最重要转换：
* 调整大小/裁剪: A.RandomCrop, A.RandomResizedCrop
* 基本几何图形: A.HorizontalFlip
* 正则化：A.CoarseDropout
* 缩放/旋转：A.Affine

常见用法模式：
* 经常应用： A.Resize(height=224, width=224, p=1.0)
* 偶尔应用： A.HorizontalFlip(p=0.5)
* 随机范围: A.RandomBrightnessContrast(brightness_limit=0.2, contrast_limit=0.2, p=0.8)

#### 1.1.1 基本转换用法    

应用单个变换
```
import albumentations as A
import cv2
import numpy as np 

image = np.random.randint(0, 256, (100, 100, 3), dtype=np.uint8)
transform = A.HorizontalFlip(p=1.0)
transformed_data = transform(image=image)
transformed_image = transformed_data['image']
print(f"Original shape: {image.shape}, Transformed shape: {transformed_image.shape}")
```
转换返回格式
所有变换都会返回一个字典，而不仅仅是变换后的图像：
```
result = transform(image=image)
transformed_image = result['image']

result = transform(image=image, mask=mask)
new_image = result['image']
new_mask = result['mask']
```

#### 1.1.2 核心转换概念    
概率参数p    
每个变换都有一个关键参数p，控制着调用变换时应用该变换的概率    
概率值：
* p=1.0 变换始终应用
* p=0.0 变换永远不应用
* p=0.5 变换有50%的概率应用
```
import albumentations as A
always_flip = A.HorizontalFlip(p=1.0)
maybe_flip = A.HorizontalFlip(p=0.5)
never_flip = A.HorizontalFlip(p=0.1)
```
这种随机性使你能够在数据增强流程中引入多样性

参数采样和范围     
除了p概率之外，许多变换还会通过接受某些参数的值范围(min_value, max_value)
来引入变异性
```
import albumentations as A
brightness_contrast = A.RandomBrightnessContrast(
    brightness_range=(-0.2, 0.3), 
    contrast_range=(-0.1, 0.1), 
    p=1.0)
rotation_transform = A.Rotate(
    angle_range=(-15, 15),
    p=0.7
)
fixed_blur = A.GaussianBlur(blur_range=(3, 3), p=1.0)
random_blur = A.GaussianBlur(blur_range=(1, 5), p=1.0)
```
关键点： 每次调用变换时，都会从范围内采样新的随机值，即使使用同一个变换实例，也会产生不同的变化。

#### 1.1.3 转换类别    
##### 1.1.3.1 技术类别    
像素变换     
仅修改图像本身的像素值，不改变图像的几何形状或空间排列     
对目标的影响：这些变换仅影响图像类目标(图像、多图像集、体数据)。不会修改掩码、边界框或关键点。    
常见的像素变换:
* 颜色调整： 随机亮度对比度、色相饱和度、颜色抖动
* 模糊效果：高斯模糊，运动模糊，散焦
* 噪声: 高斯噪声、ISO噪声、乘性噪声
* 压缩：图像压缩

空间变换    
修改图像的空间属性——其几何形状、大小或方向
对目标的影响：由于这些改变会改变几何形状，因此会影响所有空间目标：图像、掩膜、边界框、关键点和体数据。所有目标都会进行一致的变换以保持对齐    
常见的空间变换：
* 翻转：水平翻转、垂直翻转
* 旋转：旋转、安全旋转
* 调整大小：调整大小、随机缩放、随机裁剪
* 几何变形：仿射变换、透视变换、弹性变形
* 裁剪方式：随机裁剪、居中裁剪、BBoxSafe随机裁剪

##### 1.1.3.2 实用分类
1. 基本要素
**始终从以下几点开始**：
* 尺寸归一化: 随机裁剪、随机调整大小裁剪、最小最大尺寸
* 基本不变性：水平翻转(对大多数自然图像安全)
```
essential_pipeline = A.Compose([
    A.RandomCrop(height=224, width=224, p=1.0),
    A.HorizontalFlip(p=0.5)
])
```

2. 高影响力正则化(添加下一步)    
已被证实能提高泛化能力：
* Dropout变体：CoarseDropout、Erasing 
* 缩放/旋转: 仿射(先考虑保守范围)

improved_pipeline = A.Compose([
    A.RandomCrop(height=224, width=224, p=1.0),
    A.HorizontalFlip(p=0.5),
    A.CoarseDropout(num_holes_range=(1, 8), hole_height_range=(0.1, 0.15), hole_width_range=(0.1, 0.15), p=0.5),
    A.Affine(scale=(0.8, 1.2), rotate=(-15, 15), p=0.7),
])

3. 特定领域增强
对具体旋转对称性的航拍/医学图像：
   * 正方形对称： 应用正方形的所有8种旋转/翻转方式
对于对颜色敏感的任务
   * RandomBrightnessContrast: 模拟光照变化
   * ColorJitter: 全面的色彩增强
为了提高对模糊/噪声的鲁棒性：
   * 高斯模糊：相机焦点变化
   * 高斯噪声：传感器噪声模拟
为了降低对颜色的依赖性
   * ToGray: 力形状/纹理学习
   * 通道丢失：部分颜色移除

4. 高级/专业级(谨慎使用)
针对特定领域或基本增强功能不足以满足需求的情况：
    * 医学成像：弹性变换、网格畸变
    * 天气模拟：随机太阳耀斑、随机降雨、随机雾
    * 领域自适应：FDA、直方图匹配

##### 1.1.3.3 混合变换
有些变换同时结合像素和空间上的修改： 
```
mixed_transform = A.Compose([
    min_max_height=(50, 100),
    size=(80, 80),
    p=1.0
])
```

### 1.1.4 实际案例
#### 1.1.4.1 逐步构建管道
关键原则：从简单开始，逐步增加复杂性，并在每次增加后测试验证性能

步骤1： 最小基线
```
import albumentations as A
import numpy as np 
baseline_pipeline = A.Compose([
    A.RandomCrop(height=224, width=224, p=1.0),
    A.HorizontalFlip(p=0.5)
])

image = np.random.randint(0, 256, (300, 300, 3), dtype=np.uint8)
result = baseline_pipeline(image=image)
print(f"Original: {image.shape}, Augmented: {result['image'].shape}")
```

步骤2： 添加已验证的正则化
添加已知能耐提高泛化能力的变换：
```
enhanced_pipeline = A.Compose([
    A.RandomCrop(height=224, width=224, p=1.0),
    A.HorizontalFlip(p=0.5),
    A.CoarseDropout(num_holes_range=(1, 8), hole_height_range=(0.1, 0.15), hole_width_range=(0.1, 0.15), p=0.5),
    A.Affine(scale=(0.8, 1.2), rotate=(-15, 15), p=0.7),
])
```

步骤3： 特定领域的添加
```
domain_pipeline = A.Compose([
    A.RandomCrop(height=224, width=224, p=1.0),
    A.HorizontalFlip(p=0.5),
    A.CoarseDropout(num_holes_range=(1,8), hole_height_range=(0.1, 0.15), hole_width_range=(0.1, 0.15), p=0.5),
    A.Affine(scale=(0.8, 1.2), rotate=(-15, 15), p=0.7),
    A.RandomBrightnessContrast(brightness_range=(-0.2, 0.2), contrast_range=(-0.2, 0.2), p=0.6),
    A.OneOf([
        A.GaussianBlur(blur_range=(1,3), p=1.0),
        A.GaussNoise(var_limit=(10.0, 50.0), p=1.0)
    ], p=0.3)
])
```
#### 1.1.4.2 与多个目标协同工作
使用模板、边界框或关键点时，空间变换会对所有目标产生一致的影响：
```
import albumentations as A
import numpy as np 
image = np.random.randint(0, 256, (300, 300, 3), dtype=np.uint8)
mask = np.random.randint(0, 5, (300, 300), dtype=np.uint8)
spatial_pipeline = A.Compose([
    A.RandomCrop(height=224, width=224, p=1.0),
    A.HorizontalFlip(p=0.5),
    A.RandomBrightnessContrast(p=0.5)
])
result = spatial_pipeline(image=image, mask=mask)
print(f"Image shape: {result['image'].shape}")
print(f"Mask shape: {result['mask'].shape}")
print("Spatial alignment maintained between image and mask")
```
#### 1.1.4.3 验证策略
```
import albumentations as A

conservative = A.Compose([
    A.RandomCrop(height=224, width=224, p=1.0),
    A.HorizontalFlip(p=0.3),
    A.RandomBrightnessContrast(brightness_range=(-0.1, 0.1), contrast_range=(-0.1, 0.1), p=0.3)
])

moderate = A.Compose([
    A.RandomCrop(height=224, width=224, p=1.0),
    A.HorizontalFlip(p=0.5),
    A.CoarseDropout(num_holes_range=(1, 4), hole_height_range=(0.08, 0.12), hole_width_range=(0.08, 0.12), p=0.4),
    A.Affine(scale=(0.9, 1.1), rotate=(-10, 10), p=0.5),
    A.RandomBrightnessContrast(brightness_range=(-0.2, 0.2), contrast_range=(-0.2, 0.2), p=0.5),
])

aggressive = A.Compose([
    A.RandomCrop(height=224, width=224, p=1.0),
    A.HorizontalFlip(p=0.7),
    A.CoarseDropout(num_holes_range=(1, 8), hole_height_range=(0.1, 0.15), hole_width_range=(0.1, 0.15), p=0.6),
    A.Affine(scale=(0.7, 1.3), rotate=(-20, 20), p=0.7),
    A.OneOf([
        A.RandomBrightnessContrast(brightness_range=(-0.3, 0.3), contrast_range=(-0.3, 0.3), p=1.0),
        A.ColorJitter(brightness_range=(0.7, 1.3), contrast_range=(0.7, 1.3), saturation_range=(0.7, 1.3), hue_range=(-0.1, 0.1), p=1.0),
    ], p=0.6),
])
```

按照以下顺序逐步构建您的管道：
1. 尺寸标准化——先裁剪或调整大小(始终如此)
2. 基本几何不变性——水平翻转、方形对称(适用于航空/医疗领域)
3. Dropout/遮挡——CoarseDropout、ConstrainedCoarseDropout 影响巨大!
4. 降低颜色依赖性——ToGray、通道丢弃(如需要)
5. 仿射变换——用缩放/旋转的仿射变换
6. 领域特定——针对您的用例的专用转换
7. 归一化——标准或样本特定(始终最后进行)

依赖链： 
    分辨率 -> 几何形状 -> 遮挡 -> 颜色 -> 域变化 -> 归一化     
每一步都依赖前一步的完整

没有一个公式能够直接给出最优的数据增强流程。     
但存在一个可靠的方法，可以让你获得一个相当不错的流程     
需要记住三点：
1. 先从问题入手，而不是从变换入手。我的模型需要对哪些训练数据未涵盖的情况保持不变？    
2. 精准测量至关重要，汇总指标会误导人。按类别细分、在特定条件下进行鲁棒性测试以及按条件进行切片，才是区分外观良好的流程和真正能在生产环境中有效运行的流程的关键所在。
3. 将数据处理流程视为一个动态的产物。数据会变、模型会变、部署条件也会变——数据处理流程必须随之改变，否则它会悄然从资产变成累赘。

### 1.1.5 完整的管道示例

分类     
分类任务对数据增强的容错性最高
```
import albumentations as A

train_transform = A.Compose([
    A.SmallestMaxSize(max_size_hw=(256, 256), p=1.0),
    A.RandomCrop(height=224, width=224, p=1.0),
    A.HorizontalFlip(p=0.5),
    A.Affine(scale=(0.8, 1.2), rotate=(-15, 15), balanced_scale=True, p=0.5),
    A.OneOf([
        A.CoarseDropout(num_holes_range=(3, 8),
                        hole_height_range=(0.05, 0.15),
                        hole_width_range=(0.05, 0.15), p=1.0),
        A.GridDropout(ratio=0.4, unit_size_range=(0.05, 0.15), p=1.0),
    ], p=0.4),
    A.OneOf([
        A.ToGray(p=1.0),
        A.ChannelDropout(p=1.0),
    ], p=0.1),
    A.PhotoMetricDistort(brightness_range=(0.8, 1.2), contrast_range=(0.8, 1.2),
                         saturation_range=(0.7, 1.3), hue_range=(-0.05, 0.05), p=0.5),
    A.GaussianBlur(blur_range=(3, 5), p=0.1),
    A.Normalize(),
], seed=137)

val_transform = A.Compose([
    A.SmallestMaxSize(max_size_hw=(256, 256), p=1.0),
    A.CenterCrop(height=224, width=224, p=1.0),
    A.Normalize(),
], seed=137)
```

目标检测      
检测领域特有的一个微妙之处在于： 空间变换会悄然改变标签分布，而不仅仅是图像本身
```
import albumentations as A

train_transform = A.Compose([
    A.LetterBox(size=(640, 640), fill=0, p=1.0),
    A.HorizontalFlip(p=0.5),
    A.Affine(scale=(0.5, 1.5), balanced_scale=True, p=0.5),
    A.CoarseDropout(num_holes_range=(3, 8),
                    hole_height_range=(0.05, 0.15),
                    hole_width_range=(0.05, 0.15), p=0.3),
    A.ColorJitter(brightness_range=(0.7, 1.3), contrast_range=(0.7, 1.3),
                  saturation_range=(0.6, 1.4), hue_range=(-0.05, 0.05), p=0.5),
    A.MotionBlur(blur_range=(5, 5), p=0.1),
    A.Normalize(),
], bbox_params=A.BboxParams(coord_format='pascal_voc', min_visibility=0.3),
   seed=137)

val_transform = A.Compose([
    A.LetterBox(size=(640, 640), fill=0, p=1.0),
    A.Normalize(),
], bbox_params=A.BboxParams(coord_format='pascal_voc', min_visibility=0.3),
   seed=137)
```

语义分割    
分割的关键约束是掩码完整性——每个像素都有一个类别标签，而空间变换过程中的插值可能会在边界处产生无效的类别索引。
```
import albumentations as A
import cv2

train_transform = A.Compose([
    A.RandomCrop(height=512, width=512, pad_if_needed=True, p=1.0),
    A.HorizontalFlip(p=0.5),
    A.Affine(scale=(0.8, 1.5), rotate=(-10, 10), balanced_scale=True, p=0.5),
    A.CoarseDropout(num_holes_range=(3, 8),
                    hole_height_range=(0.05, 0.2),
                    hole_width_range=(0.05, 0.2), p=0.3),
    A.PhotoMetricDistort(brightness_range=(0.8, 1.2), contrast_range=(0.8, 1.2),
                         saturation_range=(0.75, 1.25), hue_range=(-0.03, 0.03), p=0.5),
    A.GaussNoise(std_range=(0.02, 0.08), p=0.1),
    A.Normalize(),
], seed=137)

val_transform = A.Compose([
    A.PadIfNeeded(min_height=512, min_width=512,
                  border_mode=cv2.BORDER_CONSTANT, fill=0, p=1.0),
    A.CenterCrop(height=512, width=512, p=1.0),
    A.Normalize(),
], seed=137)

```

### 1.2 管道(组成多个增强功能)
#### 1.2.1 定义一个简单的管道
可以通过A.Compose来定义一个管道 
```
import albumentations as A
import cv2 
import numpy as np 
image = np.random.randint(0, 256, (100, 100, 3), dtype=np.uint8)
pipeline = A.Compose([
    A.HorizontalFlip(p=0.5),
    A.RandomBrightnessContrast(p=0.8),
    A.GaussianBlur(p=0.3)
])
transformed_data = pipeline(image=image)
transformed_image = transformed_data['image']
print(f"Original shape: {image.shape}, Transformed shape: {transformed_image.shape
```
#### 1.2.2 组合参数
transforms列表是唯一必需的参数，但A.Compose还接受其他几个参数来配置其行为
```
compose = A.Compose(
    transforms,  # 必需，按顺序应用增强实例列表
    bbox_params=None,  # 目标特定参数: 用于处理边界框的配置
    keypoint_params=None,  # 目标特定参数：用于处理关键点的配置
    additional_targets=None, # 目标特定参数：定义额外输入的格式(如'mask'、'depth')
    p=1.0,  # 浮点数，应用变化组合的概率 随机检查<p 失败，返回原数据，始终应用该序列： 1.0
    is_check_shapes=True, # 验证和质量控制：执行形状一致性检查
    strict=False, # 验证和质量控制：严格模式下，任何不兼容的输入都会引发错误
    mask_interpolation=None, # 高级配置：如果设置OpenCV插值标志，则会覆盖管道中所有适用几何变换中掩码使用的插值方式
    seed=None, # 高级配置， 默认无固定种子，设置后，可复现的随机序列
    save_applied_params=False, # 高级配置，保存每次实际参数结果到字典中
)
```
灰度图像预处理
Comose包括对灰度图像进行自动预处理，以确保与所有变换兼容：
* 转换要求：Albumentations中所有单独转换都要求灰度图像具有通道尺寸(H, W, 1)
* 编写便捷性： 自动处理(H, W) (H, W, 1)格式
* 通道维度处理：如果传递灰度图没有通道维度(H, W)，则Compose在预处理期间会自动添加一个通道维度，使其成为(H, W, 1)
* 后期处理清理： 如果添加了通道维度，则会在输出中自动将其删除，以保持原始格式
* 对用户透明：此过程自动完成，可传任一格式灰度图像
#### 1.2.3 动态管道修改
向管道添加转换    
使用 + 来添加
```
import albumentations as A  
base_pipeline = A.Compose([
    A.RandomCrop(height=256, width=256),
    A.HorizontalFlip(p=0.5),
], bbox_params=A.BboxParams(format='pascal_voc', label_fields=['labels']))
extended_pipeline = base_pipeline + A.RandomBrightnessContrast(p=0.5)
extended_pipeline = base_pipeline + [A.GaussianBlur(p=0.2), A.Rotate(angle_range=(-15, 15))]
extended_pipeline = A.Resize(height=512, width=512) + base_pipeline 
extended_pipeline = [A.Resize(height=512, width=512), A.Normalize()] + base_pipeline 
print(f"bbox_params preserved: {extended_pipeline.processors.get('bboxes') is not None}")
```
从管道中移除转换    
使用 - 来移除
```
import albumentations as A
flip_transform = A.HorizontalFlip(p=0.5)
brightness_transform = A.RandomBrightnessContrast(p=0.3)
blur_transform = A.GaussianBlur(p=0.2)
pipeline = A.Compose([
    A.RandomCrop(height=256, width=256),
    flip_transform,
    brightness_transform,
    blur_transform,
])
reduced_pipeline = pipeline - A.HorizontalFlip(p=0.5)
another_flip = A.HorizontalFlip(p=0.8)
pipeline_with_another = A.Compose([
    A.RandomCrop(224, 224),
    another_flip,
])
reduced_again = pipeline_with_another - A.HorizontalFlip
further_reduced = reduced_pipeline = A.RandomBrightnessContrast - A.GassianBlur
```
*重要提示：基于类别的移除*    
该运算符按类类型 - 移除转换，而不是按实例对象移除。这意味着:
* ✅ 传递转换类（例如，A.HorizontalFlip）以移除该类的任何实例
* ✅ 无需存储转换实例 - 只需使用类名即可
* ⚠️如果存在多个该类类型，则仅删除第一个。
```
pipeline = A.Compose([
    A.Resize(224, 224), 
    A.HorizontalFlip(p=0.5),
])
modified = pipeline - A.HorizontalFlip
complex_pipeline = A.Compose([
    A.RandomCrop(256, 256),
    A.HorizontalFlip(p=0.5),
    A.RandomBrightnessContrast(p=0.3),
    A.GaussianBlur(p=0.2),
])
without_flip = complex_pipeline - A.HorizontalFlip
pipeline = A.Compose([
    A.HorizontalFlip(p=0.5),
    A.VerticalFlip(p=0.5),
    A.HorizontalFlip(p=0.5),
])
modified = pipeline - A.HorizontalFlip
```

实际案例    
逐步构建管道  
```
import albumentations as A
geometric_pipeline = A.Compose([
    A.RandomCrop(height=256, width=256),
    A.HorizontalFlip(p=0.5),
])
training_pipeline = geometric_pipeline + [
    A.RandomBrightnessContrast(p=0.5),
    A.GaussianBlur(p=0.2),
]
robust_pipeline = training_pipeline + [
    A.GaussNoise(p=0.2),
    A.GaussianBlur(p=0.2),
]
```
有条件管道建设：
```
import albumentations as A

# Base pipeline
pipeline = A.Compose([A.RandomCrop(height=256, width=256)])

# Add transforms based on conditions
if use_geometric_augmentations:
    pipeline = pipeline + [A.HorizontalFlip(p=0.5), A.Rotate(angle_range=(-15, 15))]

if use_color_augmentations:
    pipeline = pipeline + A.RandomBrightnessContrast(p=0.3)

if add_noise:
    pipeline = pipeline + A.GaussNoise(p=0.1)
```
转换替换：
```
import albumentations as A

# Original pipeline with specific transform instance
old_blur = A.GaussianBlur(blur_range=(3, 3), p=0.2)
pipeline = A.Compose([
    A.RandomCrop(height=256, width=256),
    A.HorizontalFlip(p=0.5),
    old_blur,
])

# Replace the blur transform with a different configuration
new_blur = A.GaussianBlur(blur_range=(7, 7), p=0.4)
updated_pipeline = pipeline - old_blur + new_blur
```
#### 1.2.4 管道中概率是如何运作的
当调用使用以下方式创建的管道时 A.Compose:
1. 它按照提供的顺序遍历列表中的变换
2. 对列表中的每个p变换，检查其各自的值
3. 如果对特定变换的随机检查通过(基于其p)，则该变换将应用于数据的当前状态
4. 如果检查失败，则跳过该特定转换，管道继续执行下一个转换

这意味着在对管道的任何给定调用中，实际应用的可能是已定义变换的不同子集，由它们各自的概率控制

> **重要提示：** A.Compose本身没有顶级p参数来控制整个管道是否应用。根据具体p值来决定是否激活它们     
> 如果要将变换分组到单个概率下，请改用 Sequential

#### 1.2.5 高级合成工具
仅应用一个变换：    
A.OneOf: 接受一个变换列表，并应用其中唯一一个变换，也可根据p取值随机选择
```
import albumentations as A 
pipeline = A.Compose([
    A.OneOf([
        A.GuaussianBlur(p=1.0),
        A.MontionBlur(p=1.0),
    ], p=0.9),
    A.HorizontalFlip(p=0.5),
])
```

部分应用变换的随机子集：    
A.SomeOf: 接受一个变换列表和一个数字n。     
主要特点： 
* 转换数量（n）：这可以是固定的整数或元组。如果是元组，则每次调用时选择的(min_n, max_n)转换数量将是在该范围内均匀随机选择的整数（包含该范围和整数）。[min_n, max_n]
* 选择方法（replace）：默认情况下（replace=False），变换选择不放回。如果replace=True指定了选择方法，则变换选择有放回。
* 均匀选择：变换从列表中均匀随机选择——每个变换被选中的机会均等，无论其具体p值如何。
* 主概率( p)：控制SomeOf代码块执行的概率。如果此检查失败，则不会考虑其中的任何转换。
* 个体概率：至关重要的是，在SomeOf选择转换后，它会尝试应用每个转换。每个选定的转换只有在其自身概率p值满足条件时才会应用。p列表中的值在选择后不会被忽略。
```
import albumentations as A

pipeline = A.Compose([
    # Attempt to apply between 1 and 3 of the following transforms (inclusive).
    # Selection is without replacement (default).
    # This SomeOf block has a 90% chance of being entered.
    A.SomeOf([
        A.GaussianBlur(p=0.5), # If selected, 50% chance to apply
        A.RandomBrightnessContrast(p=1.0), # If selected, 100% chance to apply
        A.HueSaturationValue(p=0.8), # If selected, 80% chance to apply
        A.Sharpen(p=0.3), # If selected, 30% chance to apply
    ], n=(1, 3), p=0.9), # Select 1 to 3 transforms, 90% of the time
    A.HorizontalFlip(p=0.5)
])

# When pipeline(image=...) is called:
# - 90% chance:
#     1. SomeOf selects N transforms (N between 1 and 3) randomly from the list.
#     2. It then iterates through these N selected transforms.
#     3. For each selected transform, it applies it *only if* that transform's individual `p` value passes its random check.
# - 10% chance: None of the transforms in SomeOf are considered.
# - Independently, 50% chance HorizontalFlip is applied.
```

A.OneOrOther: 接受两个变换和一个概率p。 
```
import albumentations as A 
pipeline = A.Compose([
    A.OneOrOther(
        first=A.HorizontalFlip(p=1.0),
        second=A.Rotate(angle_range=(-30, 30), p=1.0),
        p=0.5
    ),
    A.RandomBrightnessContrast(p=0.8),
])
```

随机顺序:以随机序列应用变换
A.RandomOrder
```
import albumentations as A
pipeline = A.Compose([
    A.RandomOrder([
        A.HorizontalFlip(p=0.5),
        A.RandomBrightnessContrast(p=0.8),
    ]),
    A.GaussianBlur(p=0.2),
])
```

选择性通道变换     
A.SelectiveChannelTransform 指定要影响的通道(通过索引)    
一个常见的用例是处理多通道数据     
```
import albumentations as A
import numpy as np 
image_rgba = np.random.randint(0, 256, (100, 100, 4), dtype=np.uint8)
pipeline = A.Compose([ 
    A.SelectiveChannelTransform(
        A.GaussianBlur(p=1.0),
        channels=[0, 1, 2],
        p=1.0
    ),
    A.HorizontalFlip(p=0.5),
])

transformed_data = pipeline(image=image_rgba)
transformed_image = transformed_data['image']
```

顺序:具有单一概率的组变换    
A.Sequential: 单一的概率控制     
A.Compose: 各自的p值决定是否激活
```
import albumentations as A
pipeline = A.Compose([
    A.Sequential([
        A.HorizontalFlip(p=1.0),
        A.RandomBrightnessContrast(p=1.0),
    ], p=0.4),
    A.GuussianBlur(p=0.2),
])
```

#### 1.2.6 嵌套组合
OneOf这些组合工具的真正强大之处Sequential在于它们可以相互嵌套，也可以在嵌套在A.Compose代码块中
```
import albumentations as A
import cv2 
import numpy as np 

image = np.random.randint(0, 256, (100, 100, 3), dtype=np.uint8)
complex_pipeline = A.Compose([
    A.HorizontalFlip(p=0.5),
    A.OneOf([
        A.Sequential([
            A.GaussianBlur(p=1.0),
            A.Sharpen(p=1.0),
        ], p=1.0),
        A.Emboss(p=1.0),
    ], p=0.8),
    A.SomeOf([
        A.RandomBrightnessContrast(p=0.7),
        A.HueSaturationValue(p=0.7),
        A.ColorJitter(p=0.7),
    ], n=(1,2), p=0.9),
    A.GaussianBlur(p=0.2),
])

transformed_data = complex_pipeline(image=image)
transformed_image = transformed_data['image']
```

### 1.3 使用目标(图像、掩码、边界框、关键点)
#### 1.3.1 快速参考



### 1.4 设置概率
## 2. 基本使用
## 3. 选择增强策略
## 4. 性能调优
## 5. 对比
